{
  "name": "graf",
  "repository": "https://github.com/codygibb/graf",
  "scripts": {
    "start": "pip3 install -r requirements.txt && bower install && gulp && gunicorn graf:app --log-file=-"
  },
  "dependencies": {
    "bower": "^1.3.12",
    "gulp": "^3.8.10",
    "gulp-clean": "^0.3.1",
    "gulp-jade": "^0.10.0",
    "gulp-less": "^1.3.5"
  }
}
